class AppConfig {
  static const defaultEsp32BaseUrl = 'http://192.168.1.12';
  static const defaultEmptyDistanceCm = 80.0;
  static const defaultFullDistanceCm = 30.0;
  static const defaultPollInterval = Duration(minutes: 2);

  const AppConfig._();
}
